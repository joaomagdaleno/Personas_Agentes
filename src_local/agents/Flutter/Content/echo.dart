import 'package:personas_agentes/core/base.dart';

/**
 * 📢 Dr. Echo — PhD in System Feedback & Content Integrity
 * Generated by Sovereign Forge.
 */
class EchoPersona extends BaseActivePersona {
  EchoPersona({String? projectRoot}) : super(projectRoot) {
    this.name = "Echo";
    this.emoji = "📢";
    this.role = "PhD Communication Specialist";
    this.    phdIdentity = "System Feedback & Content Integrity",
    stack = "Flutter",
    healingPrompt = "Fix the following logging or observability issue. Ensure structured logging, proper trace context propagation, and consistent log levels. Return ONLY the corrected code or a specific diff.";
  }

  @override
  Future<List<dynamic>> execute(ProjectContext context) async {
    this.setContext(context);
    return await this.performAudit();
  }

  @override
  Map<String, dynamic> getAuditRules() {
    return {
      'extensions': [".dart"],
      'rules': [
        // [
          {
                    "regex": "console\\.log\\(",
                    "issue": "Feedback: console.log detectado. Use o Logger PhD para rastreabilidade soberana.",
                    "severity": "medium"
          }
]
      ],
    };
  }

  Future<void> performAudit() async {
    print("💙 [${name}] Starting fleet-wide audit via Hub PhD...");
    // delegation logic here using a gRPC client stub
  }

  void reasonAboutObjective(String objective, String file, String context) {
    print("💙 [${name}] Delegating reasoning for '${objective}' to Hub...");
  }

  void delegateHealingToHub(String file, String issue) {
    print("💙 [${name}] Delegating healing for '$issue' to Hub...");
  }
}
