import 'package:personas_agentes/core/base.dart';

/**
 * 🛡️ Dr. Strict — PhD in Type Safety & Memory Purity
 * Generated by Sovereign Forge.
 */
class StrictPersona extends BaseActivePersona {
  StrictPersona({String? projectRoot}) : super(projectRoot) {
    this.name = "Strict";
    this.emoji = "🛡️";
    this.role = "PhD Security & Type Integrity";
    this.    phdIdentity = "Type Safety & Memory Purity",
    stack = "Flutter",
    healingPrompt = "Fix the following architectural or security violation. Ensure compliance with strict PhD guidelines (e.g., remove eval, enforce type safety, sanitize inputs). Return ONLY the corrected code or a specific diff.";
  }

  @override
  Future<List<dynamic>> execute(ProjectContext context) async {
    this.setContext(context);
    return await this.performAudit();
  }

  @override
  Map<String, dynamic> getAuditRules() {
    return {
      'extensions': [".dart"],
      'rules': [
        // [
          {
                    "regex": "as any",
                    "issue": "Integridade: Cast 'as any' detectado. Viola a segurança de tipos PhD.",
                    "severity": "critical"
          },
          {
                    "regex": "!",
                    "issue": "Integridade: Operador de asserção não-nula (!) detectado. Risco de runtime panic PhD.",
                    "severity": "high"
          }
]
      ],
    };
  }

  Future<void> performAudit() async {
    print("💙 [${name}] Starting fleet-wide audit via Hub PhD...");
    // delegation logic here using a gRPC client stub
  }

  void reasonAboutObjective(String objective, String file, String context) {
    print("💙 [${name}] Delegating reasoning for '${objective}' to Hub...");
  }

  void delegateHealingToHub(String file, String issue) {
    print("💙 [${name}] Delegating healing for '$issue' to Hub...");
  }
}
