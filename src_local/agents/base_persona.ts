/**
 * Re-export from base_active_persona.ts for backward-compatible import paths.
 * Several persona files import `../../base_persona.ts` — this shim resolves them.
 */
export { BaseActivePersona } from "./base_active_persona.ts";
