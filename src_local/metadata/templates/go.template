package {{category}}

import (
	"context"
	"fmt"
	base "github.com/joaomagdaleno/Personas_Agentes/src_local/agents/Go"
)

/**
 * {{emoji}} Dr. {{name}} — PhD in {{phd_identity}}
 * Generated by Sovereign Forge.
 */
type {{name}}Persona struct {
	base.BasePersona
}

func New{{name}}Persona() *{{name}}Persona {
	return &{{name}}Persona{
		BasePersona: base.BasePersona{
			ID:    "{{id}}",
			Name:  "{{name}}",
			Emoji: "{{emoji}}",
			Role:          "{{role}}",
			Stack:         "{{stack}}",
			HealingPrompt: "{{healing_prompt}}",
		},
	}
}

// Execute performs the agent logic, delegating to the Hub PhD RuleProvider.
func (p *{{name}}Persona) Execute(ctx context.Context) {
	fmt.Printf("👷 [{{name}}] Starting fleet-wide audit via Hub PhD...\n")
	// delegation logic via gRPC
}

func (p *{{name}}Persona) ReasonAboutObjective(objective string, file string, context string) {
	fmt.Printf("👷 [{{name}}] Delegating reasoning for '%s' to Hub...\n", objective)
	// delegation logic via gRPC
}

func (p *{{name}}Persona) DelegateHealingToHub(file string, issue string) {
	fmt.Printf("👷 [{{name}}] Delegating healing for '%s' in '%s' to Hub...\n", issue, file)
	// delegation logic via gRPC
}
